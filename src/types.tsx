export type ContactObjectType = {
    id: any,
    name: string,
    email: string,
    number: string,
    address: string,
    user: string | null
}

export type UserObjectType = {
    name: string | null,
    email: string | null,
}